input CreateCategoryInput {
  name: String!
  description: String
  avatar: String
}

input UpdateCategoryInput {
  name: String
  description: String
  avatar: String
}

type Category {
  id: Int!
  name: String!
  description: String
  avatar: String
}

type Query {
  category: Category!
  categorys: [Category!]
}
